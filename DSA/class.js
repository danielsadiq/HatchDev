"use strict";
class LNode {
    constructor(value) {
        this.value = value;
        this.next = null;
    }
}
class LinkedListx {
    constructor(initialValue) {
        const newNode = new LNode(initialValue);
        this.head = newNode;
    }
    add(value) {
        const newNode = new LNode(value);
        this.head;
    }
}
